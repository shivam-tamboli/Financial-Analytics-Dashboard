# Frontend — Penta Dashboard

Vite + React + TypeScript SPA. See the [repo root README](../README.md) for full setup,
usage, and API documentation.

Quick start:

```bash
cp .env.example .env
npm install
npm run dev
```
